
let CONFIG = {
  url: 'https://front.catchme.com.cn/',
  version: '1.0.0',
  scene:2,
  token: '',
  // machine_no: wx.getStorageSync('machine_no') || 'CATCH_100024',
  machine_no:''
  // u: function () {
  //   return `?machine=${this.machine_no}&token=${this.token}`
  // },
}

export default CONFIG
